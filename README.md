# 세움디자인하우징 전시장 모델 취합 페이지

전시장 팀장님들이 보유 주택 모델 정보를 입력해 자료를 취합하는 단일 페이지 폼입니다.
입력한 자료를 바탕으로 상세페이지를 제작하는 용도로 사용합니다.

## 사용 방법
- `index.html` 파일을 브라우저로 열면 바로 사용할 수 있습니다. (별도 설치/서버 불필요)
- 모바일에서도 사진 첨부와 입력이 편하도록 반응형으로 제작되었습니다.

## 폼 항목
- 전시장 선택 (1~5전시장)
- 담당자 이름
- **주택 유형 (주택 / 쉼터 / 기타)** — 선택형
- 주택 모델명
- 주택 평형
- 주택가격
- 주택 외관사진 (여러 장 첨부 가능)
- 주택 내부사진 (여러 장 첨부 가능)

## 추가 기능
- 최하단 **「주택 모델 추가 제출하기」** 버튼
- **제출일시 자동 표기** — 제출 시각이 자동 기록되어 취합 목록에 표시됩니다.
- **제출 시 알람** — 제출 완료 토스트 안내 + 브라우저 알림(권한 허용 시)
- 제출 자료는 Supabase에 저장되어 모든 사용자가 「취합된 모델」 목록에서 공유·확인할 수 있습니다.
- **제출 후 사진 추가** — 이미 제출한 모델에도 외관·내부사진을 나중에 「+ 사진 추가」 버튼으로 더 올릴 수 있습니다. (다른 항목 수정 없이 사진만 추가, Supabase에 즉시 반영)
- **사진 미리보기** — 제출·추가한 사진은 외관/내부로 구분해 전부 표시됩니다. 썸네일을 누르면 크게 보기(라이트박스)로 좌우(←/→·버튼)로 넘기며 볼 수 있고, 사진별 개별 삭제도 가능합니다.

## Supabase 연동
- 사진은 Supabase Storage(`house-photos` 버킷)에 업로드되고, 해당 public URL이 DB 테이블 `house_models`에 저장됩니다.
- 테이블/버킷/권한 스키마는 `supabase/schema.sql` 참고 (Supabase SQL Editor에서 실행).
- 설정 값은 `index.html` 상단 스크립트의 `SUPABASE_URL`, `SUPABASE_KEY`에 있습니다. (publishable key는 공개 가능하며 RLS로 보호됨)
- **제출 후 사진 추가/개별 삭제** 기능을 쓰려면 `schema.sql`의 `anyone can update`(update) 정책이 적용돼 있어야 합니다. (스키마에 포함되어 있으니 SQL Editor에서 실행해 주세요.)
- 취합 목록 **삭제** 기능을 쓰려면 `schema.sql`의 delete 정책 주석을 해제해 실행하세요.
